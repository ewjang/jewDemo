<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jew.mapper.LoginMapper">

    <select id="findByUserId" parameterType="String" resultType="com.jew.models.Member">
     /* LoginMapper.findByUserId [Login.xml] : 사용자 조회 */

        SELECT SEQ, USER_ID, PASSWORD, USER_NM, PHONE_NUM, EMAIL, DEL_YN, REG_ID, REG_DT, UPD_ID, UPD_DT
        FROM "MEMBER"
        WHERE USER_ID = #{userId}
          AND DEL_YN = 'N'
    </select>

</mapper>
