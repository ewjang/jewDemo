<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jew.mapper.HomeMapper">

    <select id="getQueryTest" parameterType="int" resultType="int">
     /* HomeMapper.getQueryTest [Home.xml]  : oracle cloud query test */
     
	    SELECT
	        #{menuNo} as menu_no
		FROM DUAL
			
    </select>

</mapper>